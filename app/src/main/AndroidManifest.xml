<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.laojiu.app">

    <application
        android:name=".APP"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.MainActivity"
            android:launchMode="singleTop"/>

        <activity android:name=".ui.CommentQuestionActivity" />
        <activity android:name=".ui.CommentQuestionListActivity" />
        <activity android:name=".ui.CommentQuestionSearchActivity" />

        <activity
            android:name=".ui.CommentQuestionDetailsActivity"
            android:windowSoftInputMode="adjustPan" />


    </application>

</manifest>